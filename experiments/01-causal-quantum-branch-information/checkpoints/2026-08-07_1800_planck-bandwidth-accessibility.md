# Checkpoint — 2026-08-07 18:00 EDT

## State of Experiment 01

The project has moved from a simple gravity-entanglement thought experiment to a causal quantum-receiver theory.

### Exact binary coherent Gaussian theorem

For any nontrivial finite binary coherent hybrid input and any one-mode gauge-covariant phase-insensitive Gaussian channel $\Phi_{\tau,m}$,

$$
\rho_{AB}\text{ is NPT}
\iff
m<\tau,
$$

which is exactly the channel's non-entanglement-breaking region.

A matched three-element witness detects the same boundary.

### Exact causal front

For a stationary passive Markov receiver,

$$
T_{\rm NPT}^{\min}
=\frac Rc+
\frac1{\kappa_{\rm tot}}
\ln\frac{\kappa_\Delta}
{\kappa_\Delta-\Gamma_{\rm th}}
$$

when $\kappa_\Delta>\Gamma_{\rm th}$.

### Gravitational storage map

For aligned plus quadrupoles,

$$
\Sigma_{AB}^{R}
=\frac54\sqrt{\kappa_{g,A}\kappa_{g,B}}
\frac{P(\epsilon)e^{i\epsilon}}{\epsilon^5}.
$$

The coherent storage amplitude is

$$
t_{A\to B}^{\rm store}
=-i\Sigma_{AB}^{R}/\sqrt{\kappa_{g,A}\kappa_{g,B}},
$$

so in the wave zone

$$
\eta_{\rm store}
\simeq\frac{25\mathcal O}{16(kR)^2}.
$$

### Passive vacuum ceiling

For passive nonrelativistic matter,

$$
\mathcal N_{\max}^{\rm WZ}
\lesssim
\frac{25\mathcal O}{24\zeta^2}
Q_B\mathcal C_B\beta_B^3
$$

in the weak-capture regime.

### Planck-area reconciliation

Weak bound-state graviton absorption satisfies

$$
\sigma_{\rm GR}=\Gamma_g/\omega^3\sim\ell_P^2,
$$

while ideal peak resonant storage can be $O(k^{-2})$. The reconciliation is the ultra-small fractional linewidth,

$$
\Gamma_g/\omega\sim(k\ell_P)^2,
$$

so Planck area is peak area multiplied by fractional bandwidth. Coherent loading time is

$$
T_{\rm load}\sim\Gamma_g^{-1}.
$$

### Strong self-gravity escape

Black-hole-like modes can have $Q_g=O(1)$, so Planck-suppressed bandwidth is not universal. Strong gravity improves capture but creates an accessibility/readout problem.

### Accessible receiver cascade

For Gaussian capture $(\tau_c,m_c)$ followed by readout $(\tau_r,m_r)$,

$$
\tau_{\rm tot}=\tau_c\tau_r,
$$

$$
m_{\rm tot}=\tau_rm_c+m_r,
$$

and the accessible register is NPT iff

$$
\boxed{
\tau_r(\tau_c-m_c)>m_r.
}
$$

Define

$$
\Delta_{\rm acc}=\tau_r(\tau_c-m_c)-m_r.
$$

This turns the qualitative receiver problem into a capture–coherence–accessibility criterion.

## Strongest next path

Generalize accessibility beyond Gaussian channels. Seek an operational statement in terms of the accessible branch-conditioned output states and the complementary branch records, so the same framework can cover relativistic field receivers and strong-gravity absorbers without assigning ad hoc Gaussian parameters.