name: Experiment 03 first-order crossover branch

on:
  push:
    paths:
      - 'experiments/03-superconducting-photon-flux-transduction/calculations/first_order_crossover_branch.py'
      - '.github/workflows/experiment03-first-order-crossover-branch.yml'
  workflow_dispatch:

jobs:
  branch:
    strategy:
      fail-fast: false
      matrix:
        delta: ['0.212','0.213','0.214','0.215']
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: experiments/03-superconducting-photon-flux-transduction/calculations
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: experiments/03-superconducting-photon-flux-transduction/calculations/requirements.txt
      - run: python -m pip install -r requirements.txt
      - run: python first_order_crossover_branch.py --delta ${{ matrix.delta }}
