name: Experiment 03 crossover branch topology

on:
  push:
    paths:
      - 'experiments/03-superconducting-photon-flux-transduction/calculations/crossover_branch_topology.py'
      - '.github/workflows/experiment03-crossover-branch-topology.yml'
  workflow_dispatch:

jobs:
  topology:
    strategy:
      fail-fast: false
      matrix:
        delta: ['0.212','0.213','0.214']
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: experiments/03-superconducting-photon-flux-transduction/calculations
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.13'
          cache: pip
          cache-dependency-path: experiments/03-superconducting-photon-flux-transduction/calculations/requirements.txt
      - run: python -m pip install -r requirements.txt
      - run: python crossover_branch_topology.py --delta ${{ matrix.delta }}
