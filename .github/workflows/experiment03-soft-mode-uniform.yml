name: Experiment 03 soft-mode uniform ingredients

on:
  push:
    paths:
      - 'experiments/03-superconducting-photon-flux-transduction/calculations/soft_mode_uniform_ingredients.py'
      - '.github/workflows/experiment03-soft-mode-uniform.yml'
  workflow_dispatch:

jobs:
  diagnose:
    strategy:
      fail-fast: false
      matrix:
        delta: ['0.210','0.211','0.212','0.213','0.214']
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: experiments/03-superconducting-photon-flux-transduction/calculations
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.13'
          cache: pip
          cache-dependency-path: experiments/03-superconducting-photon-flux-transduction/calculations/requirements.txt
      - run: python -m pip install -r requirements.txt
      - run: python soft_mode_uniform_ingredients.py --delta ${{ matrix.delta }}
